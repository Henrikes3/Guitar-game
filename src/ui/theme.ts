/** One color per guitar string (low E -> high e), reused by the canvas
 *  highway, lane labels and chord diagrams so a string always reads as the
 *  same color everywhere in the app. */
export const STRING_COLORS = ['#ff5566', '#ff9f43', '#ffd23f', '#4ade80', '#38bdf8', '#c084fc'];
